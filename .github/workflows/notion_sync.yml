pip install openai pyyaml requests
          python - <<EOF
          import os
          import yaml
          import requests
          from pathlib import Path
          from openai import OpenAI
          
          client = OpenAI(api_key=os.environ['OPENAI_API_KEY'])
          
          # 修正ポイント: 認証ヘッダーを環境変数から正しく読み込む
          headers = {
                "Authorization": f"Bearer {os.environ['NOTION_API_KEY']}",
                "Content-Type": "application/json",
                "Notion-Version": "2022-06-28"
          }
          
          def ask_ai_for_tags(content, title):
              allowed_categories = ["Attack Framework", "Research", "Tool", "Education"]
              allowed_importance = ["High", "Medium", "Low"]
              
              prompt = f"Project Title: {title}\nContent: {content[:1500]}\n\nTask: 1. Category from {allowed_categories}, 2. Importance from {allowed_importance}, 3. 1-3 Security Field keywords, 4. 2-3 sentences Summary in Japanese. Return JSON only."
              
              try:
                  response = client.chat.completions.create(
                      model="gpt-4o-mini",
                      messages=[{"role": "system", "content": "Return JSON with keys: category, importance, security_field, summary."},
                                {"role": "user", "content": prompt}],
                      response_format={ "type": "json_object" }
                  )
                  import json
                  return json.loads(response.choices[0].message.content)
              except Exception as e:
                  print(f"AI Error: {e}")
                  return {}
          
          def sync_to_notion(title, db_id, file_path, extra_props):
              query_url = f"https://api.notion.com/v1/databases/{db_id}/query"
              res = requests.post(query_url, headers=headers, json={"filter": {"property": "File Path", "rich_text": {"equals": file_path}}})
              data = res.json()
              
              props = {"Name": {"title": [{"text": {"content": title}}]}}
              props.update(extra_props)
          
              if data.get("results"):
                  page_id = data["results"][0]["id"]
                  # 更新(PATCH)を実行
                  patch_res = requests.patch(f"https://api.notion.com/v1/pages/{page_id}", headers=headers, json={"properties": props})
                  if patch_res.status_code != 200:
                      print(f"Update Failed: {patch_res.text}")
                  else:
                      print(f"Updated: {title} with Summary")
              else:
                  # 新規作成(POST)を実行
                  post_res = requests.post("https://api.notion.com/v1/pages", headers=headers, json={"parent": {"database_id": db_id}, "properties": props})
                  if post_res.status_code != 200:
                      print(f"Create Failed: {post_res.text}")
                  else:
                      print(f"Created: {title} with Summary")
          
          proj_db = os.environ['PROJECT_DB_ID']
          for path in Path('content/projects').rglob('*.md'):
              with open(path, 'r', encoding='utf-8') as f:
                  content = f.read()
                  if not content.startswith('---'): continue
                  parts = content.split('---')
                  data = yaml.safe_load(parts[1]) if len(parts) > 1 else {}
                  body = parts[2] if len(parts) > 2 else ""
                  
                  title = data.get('title') or path.parent.name
                  ai_decision = ask_ai_for_tags(body, title)
          
                  extra_props = {
                      "Category": {"select": {"name": ai_decision.get('category', 'Research')}},
                      "Importance": {"select": {"name": ai_decision.get('importance', 'Medium')}},
                      "Security Field": {"multi_select": [{"name": tag} for tag in ai_decision.get('security_field', ['General'])]},
                      "Summary": {"rich_text": [{"text": {"content": ai_decision.get('summary', 'No summary generated.')}}]},
                      "File Path": {"rich_text": [{"text": {"content": str(path)}}]}
                  }
                  sync_to_notion(title, proj_db, str(path), extra_props)
          EOF
